/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // basePath: '/your-base-path', // ถ้าคุณมี basePath
  // trailingSlash: true, // ถ้าคุณต้องการให้มี trailing slash
};

export default nextConfig;
